<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Post - <%= post.title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gray-100 py-10 px-5">
  <div class="max-w-5xl mx-auto bg-white shadow-xl rounded-lg p-10">

    <!-- Edit Post Form -->
    <h2 class="text-2xl font-semibold mb-6">Edit Post: <%= post.title %></h2>

    <form action="/posts/<%= post._id %>/edit" method="POST" class="space-y-6">
      <!-- Title Input -->
      <div class="flex flex-col">
        <label for="title" class="font-semibold text-gray-700">Title</label>
        <input type="text" name="title" id="title" value="<%= post.title %>" class="border rounded-lg p-3 text-gray-800" required />
      </div>

      <!-- Content Input -->
      <div class="flex flex-col">
        <label for="content" class="font-semibold text-gray-700">Content</label>
        <textarea name="content" id="content" rows="5" class="border rounded-lg p-3 text-gray-800" required><%= post.content %></textarea>
      </div>

      <!-- Submit Button -->
      <div class="flex gap-4">
        <button type="submit" class="bg-blue-500 hover:bg-blue-600 text-white py-2 px-6 rounded-md">Update Post</button>
        <a href="/profile" class="bg-gray-500 hover:bg-gray-600 text-white py-2 px-6 rounded-md">Cancel</a>
      </div>
    </form>
  </div>
</body>
</html>
