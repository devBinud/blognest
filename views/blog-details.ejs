<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post.title %> | Blog Details</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white min-h-screen p-6 flex items-center justify-center">

  <div class="w-full max-w-4xl bg-gray-800 rounded-lg shadow-lg overflow-hidden">
    
    <!-- Blog Header -->
    <div class="bg-gray-700 p-6">
      <h2 class="text-2xl font-semibold text-center mb-2 text-blue-400">Blog Details</h2>
      <h1 class="text-4xl font-bold text-center mb-2"><%= post.title %></h1>
      <div class="flex justify-center items-center space-x-3 text-gray-400 text-sm">
        <span>By: <span class="text-white font-semibold"><%= post.author && post.author.username ? post.author.username : 'Unknown Author' %></span></span>
        <span>·</span>
        <span><%= post.createdAt ? new Date(post.createdAt).toLocaleDateString() : 'Unknown Date' %></span>
      </div>
    </div>

    <!-- Blog Image -->
    <div class="h-80 overflow-hidden">
      <img src="/<%= post.image ? post.image.replace('uploads\\', 'uploads/') : 'https://via.placeholder.com/1200x600?text=No+Image' %>" 
           alt="Blog Image" 
           class="w-full h-full object-cover">
    </div>

    <!-- Blog Content -->
    <div class="p-8 text-gray-300 leading-relaxed text-lg space-y-6">
      <p><%= post.content %></p>

      <div class="text-center mt-8">
        <a href="/" class="text-blue-400 hover:text-blue-500 transition">← Back to Home</a>
      </div>
    </div>

  </div>

  <!-- Debugging Script -->
  <script>
    const imagePath = "<%= post.image ? '/' + post.image.replace('uploads\\', 'uploads/') : 'https://via.placeholder.com/1200x600?text=No+Image' %>";
    console.log({
      title: "<%= post.title %>",
      content: "<%= post.content %>",
      fullImagePath: imagePath,
      author: "<%= post.author && post.author.username %>",
      createdAt: "<%= post.createdAt ? new Date(post.createdAt).toISOString() : 'Unknown' %>"
    });
  </script>

</body>
</html>
